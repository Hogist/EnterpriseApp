<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.enterpriseapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".forgotpass"></activity>
        <activity android:name=".CancelOrderDetails" />
        <activity android:name=".MenuRequestDetails" />
        <activity android:name=".MenuChangeRequest" />
        <activity android:name=".OrderList" />
        <activity android:name=".CovidHygieneCheck" />
        <activity android:name=".MenuDetails" />
        <activity android:name=".UserAccount" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".CancleOrder"/>
        <activity android:name=".OrderNow"/>
            <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            </activity>

    </application>

</manifest>